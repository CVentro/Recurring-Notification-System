filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /logs/*.log

output.elasticsearch:
  hosts: ["http://host.docker.internal:9200"]

setup.kibana:
  host: "http://host.docker.internal:5601"